export interface Advantage {
    icon: string,
    title: string,
    body: string,
    color: string
} 

export interface WhyUs {
    title: string,
    body: string
}

export interface Service {
    number: number,
    title: string,
    body: string,
    color: string
}